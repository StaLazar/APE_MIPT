#include "temperature.h"

temp_record getAverMonthTemp(const temp_data *records, const size_t size, const temp_time time) {
    temp_record result = {0, false};
    /**
     * @todo Добавить реализацию.
     */
    return result;
}

temp_record getMinMonthTemp(const temp_data *records, const size_t size, const temp_time time) {
    temp_record result = {0, false};
    /**
     * @todo Добавить реализацию.
     */
    return result;
}

temp_record getMaxMonthTemp(const temp_data *records, const size_t size, const temp_time time) {
    temp_record result = {0, false};
    /**
     * @todo Добавить реализацию.
     */
    return result;
}

temp_record getAverAnnualTemp(const temp_data *records, const size_t size, const temp_time time) {
    temp_record result = {0, false};
    /**
     * @todo Добавить реализацию.
     */
    return result;
}

temp_record getMinAnnualTemp(const temp_data *records, const size_t size, const temp_time time) {
    temp_record result = {0, false};
    /**
     * @todo Добавить реализацию.
     */
    return result;
}

temp_record getMaxAnnualTemp(const temp_data *records, const size_t size, const temp_time time) {
    temp_record result = {0, false};
    /**
     * @todo Добавить реализацию.
     */
    return result;
}
